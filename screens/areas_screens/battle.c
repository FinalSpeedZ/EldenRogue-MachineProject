#include "battle.h"

void openBattleScreen() {
	int nInput = 0;

	int nBattleFinished = 0;

	do {
		system("cls");

		printTitleScreenHeader(); // change this to Battle

	} while (!nBattleFinished);
}